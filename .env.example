# Model Configuration
MODEL_PATH=/data/wuxiangbo/util_models/DeepSeek-OCR
CUDA_VISIBLE_DEVICES=0

# Model Performance Settings
MAX_CONCURRENCY=100
NUM_WORKERS=64
GPU_MEMORY_UTILIZATION=0.9

# Image Processing Settings
BASE_SIZE=1024
IMAGE_SIZE=640
CROP_MODE=True
MIN_CROPS=2
MAX_CROPS=6
SKIP_REPEAT=True

# Default OCR Prompt
DEFAULT_PROMPT=<image>\n<|grounding|>Convert the document to markdown.

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=False

# Temporary Directory
TEMP_DIR=/tmp/deepseek-ocr-server

# Maximum Upload Size (bytes) - 100MB
MAX_UPLOAD_SIZE=104857600
